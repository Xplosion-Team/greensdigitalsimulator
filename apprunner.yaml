version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install -e .
run:
  command: python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: PYTHONPATH
      value: src
